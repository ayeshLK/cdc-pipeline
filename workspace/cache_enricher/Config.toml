[cache_enricher.kafka]
bootstrapServers = "localhost:9092"
deadLetterTopic = "dl-meta_info"

[cache_enricher.redis]
host = "localhost"
port = 6379
cacheExpiryInterval = 3600

[cache_enricher]
livenessInterval = 60.0
